# Copy to .env (for local dev) or set these on Render
# SECRET_KEY can be any random string
SECRET_KEY=change-this-in-prod

# Database: set this to your Neon connection string on Render
# Example: postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require
DATABASE_URL=
